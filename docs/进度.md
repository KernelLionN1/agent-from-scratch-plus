# 项目进度

> 最后更新：Day6 阶段一 ✅

## 总体进度

| 天数 | 主题 | 状态 | 分支 |
|------|------|------|------|
| Day1 | 原生单Agent | ✅ 完成 | `01-solo-agent-handcode` |
| Day2 | 原生多Agent | ✅ 完成 | `02-multi-agent-handcode` |
| Day3 | 多Agent联调 + 踩坑修复 | ✅ 完成 | `03-pitfall-fix` |
| Day4 | LangChain + LangGraph 框架重构 | ✅ 完成 | `04-langchain-refactor` |
| Day5 | LLM 动态路由 + 持久化 + 对照 | ✅ 完成 | `05-dynamic-routing` |
| Day6 | 验收 + 面试复盘 | 🟡 阶段一完成 | `06-final-review` |

## Day3 完成情况（`03-pitfall-fix`）

| 阶段 | 内容 | 状态 | 产出 |
|------|------|------|------|
| 阶段一 | 联调复现 + Day1 遗留 9 坑修复 | ✅ | `tools.py`, `agent.py`, `memory.py`, `api.py` |
| 阶段二 | 失败重试 + 熔断机制（4坑） | ✅ | `llm_client.py`, `orchestrator.py`, `shared_state.py` |
| 阶段三 | Agent 行为修复（7坑） | ✅ | `roles.py`, `agents/*.py`, `message_bus.py` |
| 阶段四 | API 层联调 + 全链路测试 | ✅ | `api.py`（编排端点+全局异常处理器） |
| 阶段五 | 收尾文档 | ✅ | 进度更新、学习总结 |

**修复总览：22 个踩坑全部修复** ✅

| 来源 | 数量 | 状态 |
|------|:--:|:--:|
| Day1 | 9 | ✅ 全部修复 |
| Day2 | 13 | ✅ 全部修复 |

**Day3 核心改进：**
```
安全性：eval → safe_eval（白名单+受限命名空间）
稳定性：LLM 指数退避重试、并行容错、乐观锁重试
可靠性：消息去重、增量消费、依赖检查
可维护：全局异常处理器（AOP）、Prompt 收紧、输出格式约束
```

## Day4 完成情况（`04-langchain-refactor`）

| 阶段 | 内容 | 状态 | 产出 |
|------|------|------|------|
| 阶段一 | LangChain 单Agent 替换 | ✅ | `src/llm_client.py`（ChatOpenAI）、`src/tools.py`（@tool） |
| 阶段二 | LangGraph 多Agent 重构 | ✅ | `src/agents/`（StateGraph 替代手搓） |
| 阶段三 | 框架 vs 原生对照测试 | ✅ | 测试通过、代码量对比（手搓→框架） |
| 阶段四 | 收尾文档 | ✅ | 学习总结 |

**核心变化：** 手搓 while 循环 → create_agent()；手搓消息总线 → StateGraph；字符估算 → tiktoken

## Day5 完成情况（`05-dynamic-routing`）

| 阶段 | 内容 | 状态 | 产出 |
|------|------|------|------|
| 阶段一 | LLM 动态路由 | ✅ | `src/dynamic_agent.py`（301行）、`test/test_day5_phase1.py` |
| 阶段二 | 持久化记忆（SQLite） | ✅ | `src/session.py`（342行）、SQLiteChatMessageHistory |
| 阶段二补充 | 上下文压缩 + Prompt | ✅ | compress_light/heavy/auto、`src/prompt_builder.py`（180行） |
| 阶段三 | OpenHarness 对照分析 | ✅ | `docs/学习/05-动态路由/03-对照.md`（含生产/高并发/流中断） |
| 阶段四 | 收尾文档 | ✅ | 学习总结、全量测试 17/17 |

**Day5 核心成果：**
- 动态路由：固定边 → 条件边（LLM 自主决定下一步）
- 持久化：InMemory → SQLite + 会话管理 + 成本追踪
- 压缩：简单截断 → 两级压缩（10%轻量 + 70%重量 + 自动阈值）
- Prompt：硬编码字符串 → 三层组装（stable缓存 + context可刷新 + volatile动态）
- 能力对齐：OpenHarness 8/12 ✅

## Day6 完成情况（`06-final-review`）

| 阶段 | 内容 | 状态 | 产出 |
|------|------|------|------|
| 阶段一 | 技术收尾 | ✅ | 增量压缩、Streaming、MEMORY.md、回调（7/7测试） |
| 阶段二 | 全项目回归测试 | ⏳ | — |
| 阶段三 | 踩坑清单 + 面试题库 | ⏳ | — |
| 阶段四 | 收尾文档 | ⏳ | — |

**阶段一成果：**
- 增量压缩：全量 → 增量锚点（Factory.ai 方案）
- Streaming：`src/llm_client.py` astream() + `src/dynamic_agent.py` astream()
- MEMORY.md：`src/memory.py` FileMemoryStore（跨会话记忆）
- 回调：`src/callbacks.py` CallbackManager（8事件类型）
- 能力对齐：OpenHarness 10/12 ✅（新增 Streaming + MEMORY.md）
