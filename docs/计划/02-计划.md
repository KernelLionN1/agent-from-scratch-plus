# Day2：原生多Agent 每日计划

> 总时长：8h  |  目标：多角色分工 + 消息总线 + 串行/并行调度  
> 当日产出：基础版多Agent协作系统

---

## 阶段一：角色定义 + 消息总线（2h）

### 任务清单

- [ ] 定义 Agent 角色枚举：Planner（规划）/ Coder（编码）/ Reviewer（审核）
- [ ] 为每个角色编写专属 system prompt
- [ ] 实现简易消息总线（MessageBus）：Agent 之间通过它收发消息
- [ ] 消息格式标准化：sender / receiver / type / content

### 刻意踩坑

| 坑点 | 说明 |
|------|------|
| 消息重复消费 | 不设消息去重，同一消息被多个 Agent 重复处理 |
| 角色 Prompt 边界模糊 | 先写模糊的角色定义，观察 Agent 越权行为 |

### 产出

- [ ] `src/roles.py` — 角色定义 + prompt 模板
- [ ] `src/message_bus.py` — 消息总线
- [ ] `test_day2_phase1.py`

---

## 阶段二：串行任务分发（2.5h）

### 任务清单

- [ ] 实现 Planner Agent：接收用户需求 → 拆分成子任务
- [ ] 实现 Coder Agent：接收子任务 → 生成代码
- [ ] 实现 Reviewer Agent：接收代码 → 审核反馈
- [ ] 串行编排：Planner → Coder → Reviewer → Coder(修复)

### 刻意踩坑

| 坑点 | 说明 |
|------|------|
| 任务拆分不合理 | 不约束 Planner 拆分粒度，观察粒度过粗/过细 |
| 执行顺序混乱 | 不设依赖检查，Reviewer 在 Coder 完成前就执行 |

### 产出

- [ ] `src/agents/planner.py`
- [ ] `src/agents/coder.py`
- [ ] `src/agents/reviewer.py`
- [ ] `src/orchestrator.py` — 串行编排器
- [ ] `test_day2_phase2.py`

---

## 阶段三：并行执行 + 结果汇总（2h）

### 任务清单

- [ ] 将独立子任务改为并行执行（asyncio.gather）
- [ ] 实现结果汇总：多个 Coder 的结果合并
- [ ] 处理并行超时：某个 Agent 卡住不拖垮整体

### 刻意踩坑

| 坑点 | 说明 |
|------|------|
| 无等待逻辑 | 不写 await，主线程直接退出 |
| 输出格式不统一 | 各 Agent 返回格式不一致，汇总失败 |

### 产出

- [ ] `src/orchestrator.py` — 新增并行模式
- [ ] `test_day2_phase3.py`

---

## 阶段四：全局状态管理（1.5h）

### 任务清单

- [ ] 定义全局状态对象（SharedState）
- [ ] 各 Agent 读写共享状态（任务进度、中间结果）
- [ ] 实现状态锁/版本号，防止并发覆盖

### 刻意踩坑

| 坑点 | 说明 |
|------|------|
| 全局变量滥用 | 各 Agent 直接改全局 dict，不加锁 |
| 状态错乱 | 观察并发时 A 读到的状态被 B 覆盖 |

### 产出

- [ ] `src/shared_state.py`
- [ ] 重构各 Agent 集成 SharedState
- [ ] `test_day2_phase4.py`

---

## 收尾检查

- [ ] 所有刻意踩坑已记录
- [ ] 串行和并行两种模式均可运行
- [ ] commit 到 `02-multi-agent-handcode`
