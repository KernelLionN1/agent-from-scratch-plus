# Day7：生产化 + 平台化

> 分支：`07-rag-platform`  
> 目标：文档处理重构 + Skill 系统 + 斜杠命令 + text2sql

---

## 已完成 ✅

| 任务 | 文件 | 状态 |
|------|------|:--:|
| 文档处理子目录重构 | `src/document_processor/parser.py` | ✅ |
| MinerU 解析器（主引擎） | `MinerUParser` 类 | ✅ |
| Fallback 解析器（pymupdf） | `FallbackParser` 类 | ✅ |
| 统一入口 `convert_to_markdown()` | `src/document_processor/__init__.py` | ✅ |
| 文本分块（保留MD格式） | `split_chunks()` | ✅ |
| RAG 子目录 | `src/rag/` | ✅ |
| 向量化 + 检索 + 回答 | `src/rag/rag_engine.py` | ✅ |
| 图片描述 | `src/rag/vision.py` | ✅ |
| 智能客服方案文档 | `docs/学习/07-生产化/03-方案.md` | ✅ |
| 类 Claude 平台方案 | 同上（Skill/命令/附件） | ✅ |
| 测试 | `test/test_day7_rag.py` 4/4 ✅ | ✅ |

---

## 待执行 ⏳

### 阶段一：Skill 系统（1h）

> 扫描 .md 文件 → 解析 frontmatter → 匹配触发词 → 注入 PromptBuilder

- [ ] `src/skill_loader.py` — SkillLoader 类
  - `load_all()` — 扫描 `skills/` 目录，解析 YAML frontmatter
  - `match(user_input)` — 根据触发词匹配应加载的 skills
  - `inject_prompt(names, pb)` — 将 skill instructions 注入 PromptBuilder context 层
- [ ] 创建示例 skill：`skills/code-review.md`
- [ ] `test/test_day7_skill.py`

**产出：** `src/skill_loader.py`（~80行）、`skills/code-review.md`、测试

### 阶段二：斜杠命令系统（1h）

> 装饰器注册模式，/skill /memory /model /file

- [ ] `src/command_handler.py` — 命令注册 + 路由
  - `@register_command("name", "desc")` 装饰器
  - `handle_message(user_input, agent)` 路由函数
  - 内置命令：`/memory save|load|list`、`/model switch`、`/skill list|load`
- [ ] `test/test_day7_command.py`

**产出：** `src/command_handler.py`（~100行）、测试

### 阶段三：text2sql 安全执行（1h）

> NL → SQL → 安全校验（只允许 SELECT）→ 只读副本 → 结果解释

- [ ] `src/text2sql.py` — Text2SQL 类
  - `generate_sql(question)` — LLM 生成 SQL
  - `validate(sql)` — 正则校验（禁止 INSERT/UPDATE/DELETE/DROP）
  - `execute(sql)` — 只读副本执行
  - `explain(result)` — LLM 翻译为自然语言
- [ ] `test/test_day7_text2sql.py`

**产出：** `src/text2sql.py`（~60行）、测试

### 阶段四：收尾（1h）

- [ ] 阶段一~三 联调 + 全量测试
- [ ] `docs/学习/07-生产化/01-学习.md`（学习文档）
- [ ] 更新 `docs/进度.md`
- [ ] git push

---

## 文件规划

```
src/
├── document_processor/
│   └── parser.py              ✅ 已完成
├── rag/
│   ├── rag_engine.py          ✅ 已完成
│   └── vision.py              ✅ 已完成
├── skill_loader.py            ⬜ 待做
├── command_handler.py         ⬜ 待做
├── text2sql.py                ⬜ 待做
└── (day5/day6 原有模块)

test/
├── test_day7_rag.py           ✅ 已完成
├── test_day7_skill.py         ⬜ 待做
├── test_day7_command.py       ⬜ 待做
└── test_day7_text2sql.py      ⬜ 待做

docs/学习/07-生产化/
├── 01-学习.md                 ⬜ 待做
└── 03-方案.md                 ✅ 已完成
```
