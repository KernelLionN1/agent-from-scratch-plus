# 学习总结：6 天从零手搓 AI Agent

> 2026-06-17 | 总计 ~60h | 4 套项目代码 | 31 个踩坑 | OpenHarness 10/12 能力对齐

---

## 一、6 天做了什么

```
Day1  零基础 → 单 Agent（ReAct + Function Calling + FastAPI）
Day2  单 Agent → 多 Agent（Planner/Coder/Reviewer + 并行 + 共享状态）
Day3  修 22 个踩坑 → 生产级稳定版（重试/熔断/去重/AOP）
Day4  手搓 → 框架（LangChain + LangGraph 重构全部代码）
Day5  框架 → 工程化（动态路由 + SQLite + 压缩 + Prompt + OpenHarness 对照）
Day6  收尾 → 交付（增量压缩 + Streaming + MEMORY.md + 回调 + 面试准备）
```

## 二、能力对齐

| OpenHarness 能力 | 状态 | 实现时间 |
|------|:--:|------|
| Agent Loop Engine | ✅ | Day5 |
| Streaming Tool-Call Cycle | ✅ | Day6 |
| Parallel Tool Execution | ✅ | Day2 |
| Retry with Exponential Backoff | ✅ | Day3 |
| Token Counting & Cost Tracking | ✅ | Day4 + Day5 |
| Context Compression (Auto-Compact) | ✅ | Day5 + Day6 |
| MEMORY.md Persistent Memory | ✅ | Day6 |
| Session Resume & History | ✅ | Day5 |
| Subagent Spawning & Delegation | ✅ | Day2 |
| Prompt 分层组装 | ✅ | Day5 |
| Multi-Level Permission Modes | ⬜ | — |
| 43 Tools + MCP | ⬜ | — |

**10/12 核心能力对齐 ✅**

## 三、代码量统计

| 模块 | 文件 | 行数 |
|------|------|:--:|
| LLM 客户端 | `llm_client.py` | 305 |
| 工具系统 | `tools.py` | ~120 |
| Agent 核心 | `agent.py` / `dynamic_agent.py` | ~600 |
| 记忆管理 | `memory.py` | 768 |
| 会话管理 | `session.py` | 342 |
| Prompt 组装 | `prompt_builder.py` | 180 |
| 回调机制 | `callbacks.py` | 129 |
| API 层 | `api.py` | ~150 |
| 消息总线 | `message_bus.py` | 349 |
| 多 Agent | `agents/` | ~500 |
| **总计** | **~3,500 行** | |

## 四、踩坑统计

| 天数 | 坑数 | 重点 |
|------|:--:|------|
| Day1 | 9 | eval安全、弱prompt、token估算、死循环 |
| Day2 | 13 | 并行陷阱、消息重复、共享状态污染 |
| Day3 | — | 22坑全部修复 |
| Day4 | 3 | LangChain API 迁移 |
| Day5 | 4 | State隔离、DeepSeek格式、Property缓存 |
| Day6 | 2 | 增量锚点、广播去重 |
| **总计** | **31** | |

## 五、Java → Python 知识迁移

| 手写循环 → LangGraph StateGraph | Spring StateMachine → LangGraph |
| 全局 dict → Annotated[list, add] | List → List.addAll 追加 |
| print → CallbackManager | System.out → @EventListener |
| 硬编码字符串 → PromptBuilder | 字符串拼接 → Builder Pattern |
| 同步阻塞 → asyncio + await | Blocking → CompletableFuture |
| InMemory → SQLite | H2 → MySQL 持久化 |

## 六、生产就绪清单（含伪代码）

### 6.1 负载均衡 — Nginx

```nginx
# nginx.conf — 3 个 FastAPI 实例轮询
upstream ai_backend {
    server 127.0.0.1:8001;
    server 127.0.0.1:8002;
    server 127.0.0.1:8003;
}
server {
    listen 80;
    location /api/ {
        proxy_pass http://ai_backend;
        proxy_read_timeout 120s;  # LLM 调用可能慢
    }
}
```

启动：`uvicorn main:app --port 8001` × 3 + `nginx`

---

### 6.2 消息队列 — Kafka 统一方案

```python
# 用 Kafka 替代 MQ + 流数据（一套中间件）
from kafka import KafkaProducer, KafkaConsumer

# ── 发送任务（替代 RabbitMQ）──
producer = KafkaProducer(bootstrap_servers="kafka:9092")
producer.send("agent.tasks", json.dumps({
    "task_id": "abc", "user_id": "u1", "question": "本月销售额"
}).encode())

# ── Worker 消费 ──
consumer = KafkaConsumer("agent.tasks", group_id="ai-workers")
for msg in consumer:
    task = json.loads(msg.value)
    answer = agent.run(task["question"])
    producer.send("agent.results", json.dumps({"task_id": task["task_id"], "answer": answer}).encode())
```

**Kafka 一个集群管两类数据：**
```
agent.tasks        ← Java→Python 任务（低吞吐，秒级，MQ 职责）
sensor.readings    ← 园区传感器（高吞吐，毫秒级，Flink 消费）
agent.results      ← Python→Java 回传
dead.letter        ← 失败重试 3 次后人工处理
```

---

### 6.3 缓存 — Redis（Python + Java 双端）

```python
# ── Python AI 中台：缓存 LLM 结果 + Session ──
import redis, hashlib, json

r = redis.Redis(host="localhost", port=6379, decode_responses=True)

def cached_agent_run(question: str, ttl: int = 300) -> str:
    """相同问题 5 分钟内走缓存，不调 LLM"""
    key = f"llm:{hashlib.md5(question.encode()).hexdigest()}"
    cached = r.get(key)
    if cached:
        return json.loads(cached)  # 命中 → 零延迟
    result = agent.run(question)
    r.setex(key, ttl, json.dumps(result))  # TTL 5 分钟
    return result

# ── Session 存储（替代内存 dict）──
def save_session(session_id: str, data: dict, ttl: int = 3600):
    r.setex(f"session:{session_id}", ttl, json.dumps(data))

def load_session(session_id: str) -> dict | None:
    raw = r.get(f"session:{session_id}")
    return json.loads(raw) if raw else None
```

```java
// ── Java 业务中台：缓存接口结果（Spring Cache）──
@Service
public class AiPlatformClient {
    @Cacheable(value = "ai-responses", key = "#question.hashCode()")
    public String askAgent(String question) {
        return restTemplate.postForObject("http://ai-backend/api/chat", question, String.class);
    }
    // @Cacheable 自动：查 Redis → 命中返回 → 未命中调接口 → 存 Redis
}

// application.yml
// spring.cache.type=redis
// spring.cache.redis.time-to-live=300000  # 5分钟 TTL
```

**TTL 策略：**

| 数据类型 | TTL | 原因 |
|---------|:--:|------|
| LLM 结果缓存 | 5 min | 相同问题短期内不重复调 |
| Session | 1 hour | 会话超时自动清理 |
| 限流计数器 | 1 min | slowapi 令牌桶 |
| text2sql 结果 | 10 min | 数据库变化慢，可缓存更久 |



```python
# 用 Redis 令牌桶，每用户每分钟 100 次
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address, storage_uri="redis://localhost:6379")

@app.post("/api/chat")
@limiter.limit("100/minute")  # 超过 → HTTP 429
async def chat(req: ChatRequest):
    return await agent.run(req.message)
```

---

### 6.4 熔断 — pybreaker

```python
# 当 LLM API 连续失败 5 次 → 熔断 30 秒 → 快速失败而非堆积等待
import pybreaker

llm_breaker = pybreaker.CircuitBreaker(
    fail_max=5,          # 连续 5 次失败 → 熔断
    reset_timeout=30,    # 30 秒后半开尝试
)

@llm_breaker
def call_llm(messages):
    return llm_client.chat(messages)

# 调用时：
try:
    result = call_llm(messages)
except pybreaker.CircuitBreakerError:
    return {"error": "LLM 服务暂时不可用，请稍后重试"}
```

---

### 6.5 监控 — Prometheus + Grafana

```python
# 暴露 /metrics 端点，Prometheus 定期抓取
from prometheus_client import Counter, Histogram, start_http_server

request_count = Counter("ai_requests_total", "Total requests", ["endpoint"])
request_duration = Histogram("ai_request_seconds", "Request duration")

@app.post("/api/chat")
async def chat(req: ChatRequest):
    request_count.labels(endpoint="chat").inc()
    with request_duration.time():
        return await agent.run(req.message)

# Grafana 面板：
#   rate(ai_requests_total[5m])     → QPS 曲线
#   histogram_quantile(0.95, ...)   → P95 延迟
```

---

### 6.6 日志聚合 — ELK / Loki

```python
# 结构化 JSON 日志 → Filebeat → Elasticsearch → Kibana 搜索
import logging, json

logger = logging.getLogger("agent")
handler = logging.FileHandler("/var/log/agent.json")
handler.setFormatter(logging.Formatter(json.dumps({
    "ts": "%(asctime)s", "level": "%(levelname)s", "msg": "%(message)s"
})))
logger.addHandler(handler)

# 查询：Kibana 里搜 `level:ERROR AND msg:*timeout*`
```

---

### 6.7 Agent 实例池

```python
# 预创建 Agent，复用实例（减少 LLM 连接开销）
from queue import Queue

class AgentPool:
    def __init__(self, size=10):
        self._pool = Queue(maxsize=size)
        for _ in range(size):
            self._pool.put(create_agent())  # Day3 工厂

    def acquire(self):
        return self._pool.get()             # 阻塞等待可用实例

    def release(self, agent):
        agent.clear()                       # 清理上下文
        self._pool.put(agent)               # 归还池中

# 使用：
agent = pool.acquire()
try:
    result = agent.run(question)
finally:
    pool.release(agent)
```

---

### 6.8 多数据源 — SQLAlchemy bind

```python
# 业务库 + 向量库，同一 ORM，按 bind_key 路由
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

# 主库（业务数据 — 写）
master = create_engine("postgresql:///business", pool_size=20)
# 只读副本（查询 — 读）
readonly = create_engine("postgresql:///business_ro", pool_size=50)
# 向量库（RAG — embedding 检索）
vector = create_engine("postgresql:///vectors")

# 文字描摹的伪代码：
with Session(master) as s:
    s.execute("INSERT INTO conversations ...")         # 写主库
with Session(readonly) as s:
    rows = s.execute("SELECT * FROM messages ...")     # 读副本
with Session(vector) as s:
    rows = s.execute("SELECT * FROM chunks ORDER BY embedding <=> :vec LIMIT 5")  # 向量检索
```

---

### 6.10 完整架构图

### 6.10 完整架构图 + docker-compose 一键部署

```yaml
# docker-compose.yml — 三语言 AI 平台 + 全部中间件
services:
  # ── 基础设施 ──
  nginx:
    image: nginx:alpine
    ports: ["80:80", "443:443"]
    volumes: ["./nginx.conf:/etc/nginx/nginx.conf"]

  redis:
    image: redis:7-alpine

  postgres:
    image: pgvector/pgvector:pg16

  kafka:
    image: confluentinc/cp-kafka:latest

  elasticsearch:
    image: elasticsearch:8.12
  kibana:
    image: kibana:8.12
    ports: ["5601:5601"]
  prometheus:
    image: prom/prometheus
  grafana:
    image: grafana/grafana

  # ── 应用服务（三语言）──
  java-gateway:
    build:
      context: https://gitlab.com/your-org/java-gateway.git#main
      dockerfile: Dockerfile
    # 或私有仓库：
    # image: registry.gitlab.com/your-org/java-gateway:latest
    ports: ["8080:8080"]

  python-ai:
    build:
      context: https://gitlab.com/your-org/python-ai.git#main
    deploy:
      replicas: 3

  go-sensor:
    build:
      context: https://gitlab.com/your-org/go-sensor.git#main
    ports: ["9090:9090"]
```

```bash
# 一键部署（单园区）
docker compose up -d

# 多园区（Profiles 隔离）
docker compose -f docker-compose.yml -f docker-compose.park-a.yml up -d
docker compose -f docker-compose.yml -f docker-compose.park-b.yml up -d

# GitLab CI 自动构建（.gitlab-ci.yml）
# build-python-ai:
#   stage: build
#   script: docker build -t registry.gitlab.com/your-org/python-ai:$CI_COMMIT_SHA .
#   only: [main]
```

**如果代码在 GitLab 上：** `build.context` 直接用 `https://gitlab.com/...git#分支名`，docker compose 会自动 `git clone` 后 `docker build`。私有仓库用 `image:` 指向 GitLab Container Registry。

### 6.11 熔断 — 三语言统一

```python
# Python — pybreaker
import pybreaker
breaker = pybreaker.CircuitBreaker(fail_max=5, reset_timeout=30)
@breaker
def call_llm(messages): return llm_client.chat(messages)
```

```java
// Java — Resilience4j
@CircuitBreaker(name = "llmService", fallbackMethod = "fallback")
public String callLlm(List<Message> messages) { ... }
public String fallback(List<Message> messages, Exception e) {
    return "{\"error\": \"AI 服务暂时不可用，请稍后重试\"}";
}
```

```go
// Go — go-kit/circuitbreaker
cb := gobreaker.NewCircuitBreaker(gobreaker.Settings{MaxRequests: 5, Timeout: 30 * time.Second})
result, err := cb.Execute(func() (interface{}, error) { return callLlm(messages) })
```

### 6.12 日志聚合 — ELK（三语言统一 JSON 输出）

```yaml
# docker-compose 片段
services:
  elasticsearch: {image: elasticsearch:8.12}
  logstash: {image: logstash:8.12, volumes: ["./logstash.conf:/usr/share/logstash/pipeline/logstash.conf"]}
  kibana: {image: kibana:8.12, ports: ["5601:5601"]}
```

三语言统一输出 `{"service":"...", "lang":"...", "level":"...", "msg":"..."}` → Filebeat → Logstash → ES → Kibana。查询：`lang:python AND level:ERROR`。

### 6.13 生产级开源参考对比

| 组件 | 我们设计的 | Dify 实际 | 状态 |
|------|------|------|:--:|
| Nginx 负载均衡 | ✅ | ✅ | 一致 |
| Redis 缓存+队列 | ✅ | ✅ | 一致 |
| PostgreSQL+pgvector | ✅ | ✅ | 一致 |
| Kafka 消息总线 | ✅ | — | Dify 用 Redis 队列 |
| Prometheus+Grafana | ✅ | ✅ | 一致 |
| docker-compose 容器化 | ✅ 三语言 | Python+TS | 方向一致 |
| ELK 日志聚合 | ✅ | ⚠️ 容器日志 | 我们更全 |
| pybreaker 熔断 | ✅ | ⚠️ K8s 探针 | 互补 |
| GitLab CI/CD | ✅ | — | 新增 |

**我们设计的生产架构跟 Dify 的商业级部署高度重合。**

## 七、项目总结

```
═══════════════════════════════════════════
  Agent From Scratch Plus — 6 天项目总览
═══════════════════════════════════════════

代码量:    ~3,500 行 Python（12 个模块）
测试:      21 个测试文件，全部通过
踩坑:      34 个（9 大分类）
面试题:    15 道（概念/架构/代码/生产）
学习文档:  17 份（每天 3-4 份）

OpenHarness 能力对齐: 10/12 ✅

Day1  单 Agent ──→ Day2  多 Agent
Day3  修坑   ──→ Day4  框架重构
Day5  工程化 ──→ Day6  交付验收
═══════════════════════════════════════════
```

## 八、下一步（Day7+）

### 方向一：生产化
补全 pybreaker 熔断 + ELK 日志 + docker-compose + GitLab CI/CD → 一键多园区部署 ✅ 已在上文完成

### 方向二：平台化 — Dify/n8n + text2sql

**Dify 集成：**
```python
# Dify 提供 API，Python 中台作为 Dify 的工具节点调用
@app.post("/tool/text2sql")  # 注册为 Dify Custom Tool
async def text2sql_tool(req: Text2SQLRequest):
    sql = llm.chat(f"生成SQL: {req.question}")
    if not re.match(r"^\s*SELECT\b", sql, re.IGNORECASE):  # 安全校验
        raise HTTPException(400, "只允许 SELECT")
    result = readonly_db.execute(sql)
    return llm.chat(f"解释结果: {result}")
```

**n8n 工作流：** 可视化拖拽——定时触发 → HTTP 调 AI 接口 → 结果写数据库/发通知。适合非技术人员编排 Agent 调用链。

### 方向三（已完成 Day7 分支）：多模态 RAG

```
PDF/Word → pymupft → 文本分块 → ChromaDB 向量化 → 检索 → LLM 回答
图片 → GPT-4V/回退 → 描述文本 → 注入 RAG
```

Day7 分支 `07-rag-platform` 已实现：`src/document_processor.py` + `src/rag_engine.py` + `src/vision.py`
